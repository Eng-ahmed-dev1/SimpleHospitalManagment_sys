<Window x:Class="Hospital_man_sys.Views.Appoin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hospital_man_sys.Views"
        mc:Ignorable="d"
        Title="Appoin" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="Black"></Setter>
            <Setter Property="Foreground" Value="White"></Setter>
            <Setter Property="FontSize" Value="16"></Setter>
            <Setter Property="FontWeight" Value="ExtraBold"></Setter>
            <Setter Property="FontFamily" Value="Century"></Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Width="100">
                            <ContentPresenter 
                                VerticalAlignment="Center" 
                                HorizontalAlignment="Center" 
                                Content="{TemplateBinding Content}"></ContentPresenter>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="#27374D">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="1" 
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Center" 
                   Text="Appointments" 
                   FontSize="25" 
                   FontWeight="ExtraBold" 
                   Foreground="White"/>

        <DataGrid x:Name="AppointmentsGrid" 
                  AutoGenerateColumns="False" 
                  Grid.Row="1" 
                  Grid.Column="1"
                  IsReadOnly="True"
                  Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="Patient ID" Binding="{Binding Pat_ID}" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="Name" Binding="{Binding Patients.Name}" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="Condition" Binding="{Binding Patients.Condition}" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="DateTime" Binding="{Binding DateTime, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="*"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button Width="120" Height="40" 
                Grid.Column="1" 
                Grid.Row="2" 
                Content="Back" 
                Click="Back"></Button>
    </Grid>
</Window>